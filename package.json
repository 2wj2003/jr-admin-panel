{
  "name": "my-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:backend": "yarn --cwd api develop",
    "dev:frontend": "wait-on http://localhost:1337/admin && yarn --cwd web dev",
    "dev": "cross-env FORCE_COLOR=1 npm-run-all -l -p dev:*"
  },
  "devDependencies": {
    "cross-env": "7.0.3",
    "npm-run-all": "4.1.5",
    "wait-on": "5.2.1"
  },
  "dependencies": {
    "striptags": "^3.2.0"
  }
}
